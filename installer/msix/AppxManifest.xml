<?xml version="1.0" encoding="utf-8"?>
<!-- installer/msix/AppxManifest.xml — the Microsoft Store package.

     A template: pack.ps1 fills the three {{...}} fields. Identity name and
     publisher come from Partner Center once the app name is reserved there;
     until then the defaults in pack.ps1 give a package that sideloads for
     testing. The version is Cargo's, with the fourth part the Store keeps
     for itself set to 0.

     The same ailimits.exe that the installer ships runs here. It notices it
     is packaged (platform::is_packaged) and leaves the tray-icon registry
     write and the silent self-update to the Store, which owns both. -->
<Package
  xmlns="http://schemas.microsoft.com/appx/manifest/foundation/windows10"
  xmlns:uap="http://schemas.microsoft.com/appx/manifest/uap/windows10"
  xmlns:desktop="http://schemas.microsoft.com/appx/manifest/desktop/windows10"
  xmlns:rescap="http://schemas.microsoft.com/appx/manifest/foundation/windows10/restrictedcapabilities"
  IgnorableNamespaces="uap desktop rescap">

  <Identity
    Name="{{IDENTITY_NAME}}"
    Publisher="{{PUBLISHER}}"
    Version="{{VERSION4}}"
    ProcessorArchitecture="x64" />

  <Properties>
    <DisplayName>QuotaBar</DisplayName>
    <PublisherDisplayName>{{PUBLISHER_DISPLAY}}</PublisherDisplayName>
    <Logo>Assets\StoreLogo.png</Logo>
    <Description>A lightweight Windows 11 taskbar tool for Codex weekly and 5-hour remaining quota.</Description>
  </Properties>

  <Dependencies>
    <!-- Windows 11 only: the acrylic backdrop and the taskbar geometry the
         mini panel relies on are Windows 11 behaviour. -->
    <TargetDeviceFamily Name="Windows.Desktop" MinVersion="10.0.22000.0" MaxVersionTested="10.0.26100.0" />
  </Dependencies>

  <Resources>
    <Resource Language="en-us" />
  </Resources>

  <Applications>
    <Application Id="AiLimits" Executable="ailimits.exe" EntryPoint="Windows.FullTrustApplication">
      <uap:VisualElements
        DisplayName="QuotaBar"
        Description="Live Claude, Codex, Copilot and Antigravity usage limits on the desktop, in the tray or on the taskbar."
        BackgroundColor="transparent"
        Square150x150Logo="Assets\Square150x150Logo.png"
        Square44x44Logo="Assets\Square44x44Logo.png">
        <uap:DefaultTile
          Wide310x150Logo="Assets\Wide310x150Logo.png"
          Square71x71Logo="Assets\Square71x71Logo.png"
          Square310x310Logo="Assets\Square310x310Logo.png" />
        <uap:SplashScreen Image="Assets\SplashScreen.png" />
      </uap:VisualElements>

      <Extensions>
        <!-- Start at sign-in, the way the installer's autostart task does.
             This is the supported route for a packaged app; the user can turn
             it off under Settings > Apps > Startup, and the app never touches
             the Run key when packaged. -->
        <desktop:Extension Category="windows.startupTask" Executable="ailimits.exe" EntryPoint="Windows.FullTrustApplication">
          <desktop:StartupTask TaskId="AiLimitsStartup" Enabled="true" DisplayName="QuotaBar" />
        </desktop:Extension>
      </Extensions>
    </Application>
  </Applications>

  <Capabilities>
    <!-- A Win32 desktop app packaged as-is. -->
    <rescap:Capability Name="runFullTrust" />
  </Capabilities>
</Package>
